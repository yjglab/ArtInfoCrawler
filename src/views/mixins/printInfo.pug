mixin printInfo(info)
    - for (var i = 0; i < info.infoLength; i += 1)
        h4 Title: #{info.titles[i]} 
        img(src=`${info.thumbnailsSrc[i]}`)
        div Date: #{info.dates[i]} 
        div Detailed: #{info.details[i]}